# TODO: Fix "Lịch sử phát triển" không hiện khi chạy server

## Steps

- [x] 1. Thêm seed data cho `timeline_events`, `faqs`, `brands`, `pricing`, `reviews` vào `install.sql`
- [x] 2. Sửa `backend.js`: Khởi tạo lại scroll reveal animation sau khi thay nội dung động
- [x] 3. Kiểm tra — API `/content/bootstrap` trả về dữ liệu timeline đầy đủ (5 mốc sự kiện)
